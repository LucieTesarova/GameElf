<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <!-- Toolbar theme. -->
    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/colorgreen</item>
        <item name="android:fontFamily">@font/eaterregualr</item>
    </style>

    <!-- Menu item theme. -->
    <style name="CustomPopupStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/coloryellow</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-10dp</item>
        <item name="android:fontFamily">@font/eaterregualr</item>
    </style>

    <!-- DialogFragment theme. -->
    <style name="MyDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/coloryellow</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:fontFamily">@font/eaterregualr</item>
        <item name="android:gravity">center</item>
    </style>

     <!--Font style.xml -->
    <style name="FontTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:fontFamily">@font/eaterregualr</item>
    </style>

</resources>
